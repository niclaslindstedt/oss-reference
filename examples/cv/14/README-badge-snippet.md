<!-- Drop this line into your repository's README.md alongside the other
     status badges. The badge SVG and link both point at the workflow
     file shown in this example as `.github/workflows/dependabot.yml`,
     so for the badge to render anything meaningful that workflow file
     must already be installed in the same repo (and its CI runs must
     be public — private repos need a token-aware shield instead).

     The workflow exits 1 while any open Dependabot PR exists, so the
     badge renders red while the queue is non-empty and green once it is
     drained. Replace org/repo with your own. -->

[![Dependabot](https://github.com/niclaslindstedt/cv/actions/workflows/dependabot.yml/badge.svg)](https://github.com/niclaslindstedt/cv/actions/workflows/dependabot.yml)
